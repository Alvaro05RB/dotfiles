{
  "output": "DP-1",
  "layer": "top",
  "layer": "top",
  "position": "top",
  "modularity": true,
  "height": 20,
  "spacing": 5,

  // --- ORDEN DE LOS MÓDULOS ---
  "modules-left": [
    "custom/date",
    "cpu",
    "memory"
  ],
  "modules-center": [
    "hyprland/workspaces"
  ],
  "modules-right": [
    "temperature",
    "pulseaudio#microphone",
    "pulseaudio",
    "tray"
  ],

  // --- CONFIGURACIÓN DE MÓDULOS ---

  "hyprland/workspaces": {
    "disable-scroll": true,
    "all-outputs": true,
    "warp-on-scroll": false,
    "format": "{icon}",
    "format-icons": {
      "active": "",
      "default": "",
      "urgent": "",
      "empty": ""
    },
    "persistent-workspaces": {
      "*": 5
    },
    "on-click": "activate"
  },

  // Añadido icono y formato CPU
  "cpu": {
    "format": " {usage}%",
    "tooltip": true
  },

  // Añadido icono y formato RAM
  "memory": {
    "format": " {}%",
    "tooltip": true
  },

  "temperature": {
    // This finds the correct folder automatically every boot
    "hwmon-path-abs": "/sys/devices/platform/coretemp.0/hwmon",
    "input-filename": "temp1_input",
    "critical-threshold": 80,
    "format": "{icon} {temperatureC}°C",
    "format-icons": ["", "", ""]
  },

  "pulseaudio": {
    "format": "{icon} {volume}%",
    "format-muted": " {volume}%",
    "tooltip": true,
    "scroll-step": 1,
    "format-icons": {
        "default": ["", ""]
    },
    "on-click": "pamixer -t",
    "on-click-right": "pavucontrol --tab=3"
  },

  "pulseaudio#microphone": {
    "format": "{format_source}",
    "format-source": "",
    "format-source-muted": " ",
    "on-click": "pamixer --default-source -t",
    "on-click-right": "pavucontrol --tab=4",
    "tooltip-format": "{source_desc} // {source_volume}%"
  },

  "tray": {
    "spacing": 10
  },

  "custom/date": {
    "format": " {}",
    "tooltip-format": "{}",
    // Forzamos inglés con LC_TIME y usamos %A para el día completo
    "exec": "LC_TIME=en_US.UTF-8 date '+%A %d/%m %H:%M'", 
    "interval": 60
  }

}
